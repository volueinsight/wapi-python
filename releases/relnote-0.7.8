Changing github organisation name from wattsight to volueinsight.
Fix the authentication issue that happens when the attempt to get the token fails in a scenario where a Session object is shared among multiple threads.
